FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir --upgrade pip

COPY . /app/
RUN pip install --no-cache-dir -e .

ENV PYTHONUNBUFFERED=1
EXPOSE 8080

CMD ["python", "-m", "norman_mcp.gmail_server", "--host", "0.0.0.0", "--port", "8080"]
